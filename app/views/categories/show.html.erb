<p id="notice"><%= notice %></p>
<div class="container">
<h1 class="title"><%= @category.name %></h1>
<h2 class="subtitle">Total de productos: <%= @category.products.size%></h2>
<section class="columns">
  <section class="section">
  <div>
  <% if current_user %>
  <%= link_to 'New Product', new_product_path %>
  <% end %>
    <div class="columns">
    <% @category.products.each_with_index do |product, index| %>

    <% if index % 4 == 0 %>
 </div>
 <div class="columns">
  
 <% end %>
  <% id = product.category_id %>
  
  <div class ="column is-one-quarter">
  <div class="card ">
 
    <div class="card-image"> 
      <figure class="image is-4by3">
      <%= showImage(product) %>
      </figure>
    </div>
  
    <div class="card-content">
      <div class="media-content">
        <div class="title">
          <%= product.name %>
        </div>
        <div class="subtitle">
        Precio:
          $<%= product.price %>
        </div>
      </div>
      <div class ="content">
        <%= product.description %>
        
        <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit.
        Phasellus nec iaculis mauris.</p> 

      </div>
      <div class="card-footer">
      <%= link_to 'Show', product, class: "card-footer-item" %>
      <%= user_alter_element(@category) %>
      </div>
    </div>
  </div>
  </div>
  
  
<% end %>
</div>
</div>
</section>

</section>


</div>


